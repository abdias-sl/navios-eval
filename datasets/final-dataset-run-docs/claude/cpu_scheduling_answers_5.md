Query,Response (default mode)
"What happens if the user enters only one process in the input form?","The app still runs all algorithms, and the timeline will show only that process executing from its arrival time. The ResultsTable will display metrics for the single process."
"How does the application handle and visualize idle CPU time?","If there is a gap between process arrivals, the timeline includes 'idle' events, which are visually represented in the Gantt chart by a distinct color (e.g., gray)."
"How does the user set the time quantum for the Round Robin algorithm?","The time quantum is set via a numeric input field in the ProcessInputForm component. The value is passed to the runRoundRobin function when running the RR algorithm."
"How does the application display per-process and average metrics for each algorithm?","The ResultsTable component displays a table of metrics for each process (waiting time, turnaround time, response time) and shows the averages at the bottom."
"How does the application handle process priorities in the input form?","Each process has a priority field in the input form, but the current algorithms do not use priority for scheduling. The field is included for extensibility and possible future algorithms."
"How does the application handle user errors, such as entering negative burst times or arrival times?","The input fields for arrival time, burst time, and priority are restricted to non-negative values using the min attribute. The app prevents invalid input at the UI level."
"How does the application update the visualization and results when the process list or time quantum changes?","When the user modifies the process list or time quantum and clicks 'Run Algorithms', the CPUSchedulingVisualizer component re-runs all algorithms and updates the results and visualizations accordingly."
"How does the application structure the data for processes and scheduling results?","The app uses TypeScript interfaces: Process (for each process), TimelineEvent (for each execution segment), and SchedulingResult (containing the list of processes, timeline, and average metrics)."
"How does the application ensure a responsive and modern UI for the scheduling visualizer?","The app uses modern React patterns, Tailwind CSS for styling, and component libraries for UI elements, ensuring a responsive and visually appealing interface across devices."
"What is the main purpose of the Grocky platform?","Grocky is an AI-powered job interview preparation platform that simulates real interview scenarios using AI voice agents, providing users with real-time feedback and transcripts to help them improve."
"Which technologies are used to build Grocky?","Grocky uses Next.js for frontend and backend logic, Tailwind CSS for styling, Firebase for authentication and backend services, Vapi AI for voice agent interactions, shadcn/ui for UI components, Google Gemini for AI-based feedback, and Zod for input validation."
"How does a user start an interview simulation on Grocky?","A user logs in, navigates to the interview section, and initiates an interview session, which starts an AI voice agent using the Agent component."
"How does the Agent component manage the interview call and transcript?","The Agent component uses the Vapi SDK to start and manage the AI voice call, listens for events like call start, end, and transcript messages, and updates the UI with the current call status and transcript."
"How does Grocky provide feedback to users after an interview?","After the interview ends, the Agent component triggers the createFeedback action, which saves the transcript and navigates the user to the feedback page, where AI-generated feedback is displayed."
"How is authentication handled in Grocky?","Authentication is managed using Firebase, with the AuthForm component providing the UI for login and registration."